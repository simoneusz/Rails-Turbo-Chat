<%= search_form_for @query,
                    url: users_path,
                    method: :get,
                    html: { class: "d-flex", role: "search" } do |f|
%>
  <%= f.label :search, "Search Users" %>
  <%= f.search_field :search,
                     value: params.dig(:q, :search),
                     class: "form-control me-2",
                     placeholder: "Enter name, email, or username"
  %>
  <%= f.submit "Search" %>
<% end %>