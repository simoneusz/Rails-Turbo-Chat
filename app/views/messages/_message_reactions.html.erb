<% message.sorted_grouped_reactions.each do |emoji, reactions| %>
  <%= render partial: 'reactions/reaction',
             locals: {
               emoji: emoji,
               users: reactions.map(&:user),
               current_user: current_user,
               message:
             } %>
<% end %>
