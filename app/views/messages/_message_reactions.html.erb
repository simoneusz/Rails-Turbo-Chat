<% message.reaction_counter.each do |emoji, emoji_size| %>
  <%= render partial: "reactions/reaction", locals: { reaction_counter: { emoji:, emoji_size: }, message:,  current_user: current_user } %>
<% end %>
